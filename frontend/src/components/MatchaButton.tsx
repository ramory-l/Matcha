import React, { FC } from "react";

interface MatchaButttonProps {
  title: string;
}

const MatchaButton: FC<MatchaButttonProps> = ({ title }) => {
  return <div></div>;
};

export default MatchaButton;
